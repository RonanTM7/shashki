<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cursach">
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AchievementsActivity" />
        <activity android:name=".ProgressActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".FriendsActivity" />
        <activity android:name=".FriendsListActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".ArticlesActivity" />
        <activity android:name=".RulesActivity" />
        <activity android:name=".LegendsActivity" />
        <activity android:name=".EnterCurrentPasswordActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".EnterNewPasswordActivity" />
        <activity android:name=".ConfirmNewPasswordActivity" />
        <activity android:name=".LegendDetailActivity" />
    </application>

</manifest>